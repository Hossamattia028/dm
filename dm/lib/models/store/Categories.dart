class Categories{
  String id,name,image,main_category_id,city_id;

  Categories({this.id, this.name, this.image,this.main_category_id,this.city_id});

  // Categories.fromJson(Map<String, dynamic> jsonMap) {
  //   id = jsonMap['id'].cast<String>();
  //   name = jsonMap['name'].cast<String>();
  // }
}